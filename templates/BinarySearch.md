```java
    public static int binarySearch(int[] nums, int target) {
        int lo = 0;
        int hi = nums.size;
        while (lo < hi) {
            int mid = (lo + hi)/2;
            
        }
        return -1;       
    }
        
```